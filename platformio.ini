; ESP32 Wi-Fi Connect project configuration
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies and extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino

; Serial connection configurations
monitor_speed = 115200
upload_speed = 921600

; Serial monitor filters: decodes stack traces to exact C++ line numbers
monitor_filters = 
    esp32_exception_decoder
    time
    colorize

; Modern C++ standard
build_unflags = -std=gnu++11
build_flags = -std=gnu++17

; Add project dependencies here when needed
; lib_deps =
;     bblanchon/ArduinoJson @ ^7.0.0
;     knolleary/PubSubClient @ ^2.8